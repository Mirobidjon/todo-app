# Todo App with Go RESTful API

```shell

# create a PostgreSQL database server in a Docker container
make createdb

# migrate up database
make migrateup

# run the RESTful API server
make run

# Start PostgreSQL database container
make startdb

# Stop PostgreSQL database container
make stopdb


```